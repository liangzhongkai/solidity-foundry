repos:
  - repo: local
    hooks:
      - id: ci-checks
        name: CI checks (forge fmt, build, test)
        entry: scripts/pre-commit-ci.sh
        language: system
        pass_filenames: false
        verbose: true
